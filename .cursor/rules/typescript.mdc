---
description: TypeScript·신규 코드 언어 규칙
globs: **/*.{ts,tsx}
alwaysApply: false
---

# TypeScript

## 반드시 할 것

- 새 로직·컴포넌트·유틸은 **`.ts` / `.tsx`**로 작성한다.
- `any`를 남발하지 않고, 타입을 명시하거나 타입 추론이 명확하게 되게 작성한다.

## 하지 말 것

- **새 기능**을 **`.js` / `.jsx`만**으로 추가하지 않는다. (기존 JS 레거시는 별도 합의 시에만 점진 마이그레이션.)
